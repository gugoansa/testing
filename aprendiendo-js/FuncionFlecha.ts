const describirPersona = (nombre: string, edad: number, ciudad: string) => {
  console.log(`Hola, soy ${nombre}, tengo ${edad} años y vivo en ${ciudad}.`);
};

describirPersona("Tatao", 10, "Sydney");
